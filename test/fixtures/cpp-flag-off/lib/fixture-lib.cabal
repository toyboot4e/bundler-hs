cabal-version: 3.0
name: fixture-lib
version: 0

flag debug
  description: Off by default, so cabal would not pass -DDEBUG.
  default: False

library
  default-extensions: CPP
  cpp-options: -DMARKER=7

  if flag(debug)
    cpp-options: -DDEBUG
